The Application "MFPluggerService_TestConsole" is just a replica of window service used by 
GPS to do scheduling service.

While creating new scheduler task, we just need to focus on the class library whose (just) dll
will be used by Mindfire's plugger service at Live server.


"MFPluggerService_TestConsole" implements the same architecture as required by service.

Next Read: "MFPluggerService.pdf"